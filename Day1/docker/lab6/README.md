# Docker Lab 6 - Managing Environment Variables 🚀

## 📌 Overview
This project demonstrates how to manage **environment variables in Docker** using three different methods:

- Runtime variables (`-e`)
- Environment file (`--env-file`)
- Dockerfile default variables (`ENV`)

The application is a simple Python Flask app.

---

## 🏗️ Project Structure
